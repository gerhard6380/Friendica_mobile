<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Friendica_Mobile.ViewModel"
             xmlns:i18n="clr-namespace:Friendica_Mobile"
             x:Class="Friendica_Mobile.Views.Detail3">
    <ContentPage.BindingContext>
        <vm:NoMenuViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Margin="48">

            <Label Text="{i18n:Translate PageAppSettings}"
                   BackgroundColor="Lime"
                   VerticalOptions="Start"
                   HorizontalOptions="Center"
               TextColor="Purple"/>

            <Label Text="Test"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

            <Stepper Increment="1"
                     Minimum="0"
                     Maximum="9"
                     Value="{Binding Counter}" />
            <Label Text="{Binding Counter}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>