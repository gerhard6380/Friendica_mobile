<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:masterDetail="clr-namespace:Friendica_Mobile.Styles;assembly=Friendica_Mobile"
             xmlns:vm="clr-namespace:Friendica_Mobile.ViewModel"
             x:Class="Friendica_Mobile.Views.NoMenuDetail">
    <ContentPage.BindingContext>
        <vm:NoMenuViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
                <StackLayout>
            <Label Text="NoMenuDetail"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />

            <Label x:Name="labelSetting" />
                <Button Text="Toogle NavigationAllowed" Clicked="Handle_Clicked" />

            <Button x:Name="Test2" Text="{Binding Test}" Command="{Binding ToDetail1Command}"
                    Clicked="Handle_Clicked"/>
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>
