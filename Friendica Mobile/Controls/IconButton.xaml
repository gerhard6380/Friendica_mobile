<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:controls="clr-namespace:SeeberXamarin.Controls"
             x:Class="SeeberXamarin.Controls.IconButton"
             VerticalOptions="Start"
             HorizontalOptions="StartAndExpand">
    <ContentView.Content>
        <Grid x:Name="GridIconButton"
              HeightRequest="48">
            <StackLayout x:Name="StackButton" Orientation="Horizontal" Spacing="0">
                <controls:FontIconLabel x:Name="LabelIconButton" 
                                    Text="x"
                                        WidthRequest="48"
                                    VerticalTextAlignment="Center"
                                    HorizontalTextAlignment="Center"
                                    FontSize="20" />
                <Label x:Name="LabelTextButton"
                       LineBreakMode="NoWrap"
                       Text="x"
                       Margin="0,0,8,0"
                       IsVisible="False"
                       VerticalOptions="Center" />
            </StackLayout>
            <controls:FramelessButton x:Name="ButtonIconButton" 
                                      Clicked="IconButton_Clicked"
                                      BackgroundColor="Transparent"/>
        </Grid>
    </ContentView.Content>
</ContentView>
